import React from 'react';

const Header = () => {
  return (
    <div className={'p-6'}>
      <a href={'/'} className={'text-lg font-bold'}>
        名前検索アプリ
      </a>
      <hr />
    </div>
  );
};

export default Header;
